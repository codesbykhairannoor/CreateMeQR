const fs = require('fs');
const path = require('path');

const PSEO_ROUTES = {
  '/': 'url',
  '/wifi-qr-code-generator': 'wifi',
  '/vcard-qr-code-maker': 'vcard',
  '/text-qr-code-generator': 'text',
  '/email-qr-code-generator': 'email',
  '/sms-qr-code-generator': 'sms',
  '/phone-qr-code-generator': 'phone',
  '/event-qr-code-generator': 'event',
  '/google-maps-qr-code': 'location',
  '/whatsapp-qr-code-generator': 'whatsapp',
  '/youtube-qr-code-generator': 'youtube',
  '/instagram-qr-code-generator': 'instagram',
  '/crypto-qr-code-generator': 'crypto',
  '/app-store-qr-code-generator': 'appstore',
  '/facebook-qr-code-generator': 'facebook',
  '/twitter-qr-code-generator': 'twitter',
  '/tiktok-qr-code-generator': 'tiktok',
  '/linkedin-qr-code-generator': 'linkedin',
  '/telegram-qr-code-generator': 'telegram',
  '/snapchat-qr-code-generator': 'snapchat',
  '/discord-qr-code-generator': 'discord',
  '/spotify-qr-code-generator': 'spotify',
  '/paypal-qr-code-generator': 'paypal',
  '/venmo-qr-code-generator': 'venmo',
  '/pdf-qr-code-generator': 'pdf',
  '/google-forms-qr-code-generator': 'gforms',
  '/google-review-qr-code-generator': 'greview',
  '/image-qr-code-generator': 'image',
  '/link-in-bio-qr-code-generator': 'linkinbio',
  '/video-qr-code-generator': 'video',
  '/audio-qr-code-generator': 'audio',
  '/amazon-qr-code-generator': 'amazon',
  '/online-booking-qr-code': 'booking',
  '/file-qr-code-generator': 'file',
  '/barcode-generator': 'barcode',
  '/scan-qr': 'scanqr',
};

const LANGS = [
  'en', 'id', 'es', 'fr', 'de', 'pt', 'zh', 'ja', 'hi', 'ko', 'ar', 
  'ru', 'it', 'tr', 'nl', 'pl', 'sv', 'vi', 'th', 'el', 'cs', 'da', 
  'fi', 'no', 'hu', 'ro', 'uk', 'ms', 'tl', 'bn'
];

function robustSlugify(text) {
  if (!text) return '';
  return text.toString().toLowerCase()
    .replace(/\s+/g, '-')
    // Remove characters that could break URLs, but preserve standard foreign letters
    .replace(/[^a-z0-9\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF\-]+/g, '')
    .replace(/\-\-+/g, '-')
    .replace(/^-+|-+$/g, '');
}

async function run() {
  console.log("Generating Localized SEO Slugs for 30 languages instantly...");
  
  const localizedRoutes = {};
  const routeToToolMap = {};
  
  const localesDir = path.join(__dirname, 'public', 'locales');
  
  for (const lang of LANGS) {
    localizedRoutes[lang] = {};
    routeToToolMap[lang] = {};
    
    let types = {};
    let data = {};
    const filePath = path.join(localesDir, lang, 'translation.json');
    if (fs.existsSync(filePath)) {
      data = JSON.parse(fs.readFileSync(filePath, 'utf8'));
      types = data.types || {};
    }
    
    for (const [slug, type] of Object.entries(PSEO_ROUTES)) {
      if (slug === '/') {
        localizedRoutes[lang][type] = '/';
        routeToToolMap[lang]['/'] = type;
        continue;
      }
      
      if (lang === 'en') {
        localizedRoutes[lang][type] = slug;
        routeToToolMap[lang][slug] = type;
        continue;
      }
      
      const translatedTitle = types[type] || '';
      let translatedSlug = robustSlugify(translatedTitle);
      
      // If it fails to slugify (e.g. some extremely strict languages) or is empty, fallback to english slug
      if (!translatedSlug || translatedSlug.length < 2) {
        translatedSlug = slug.substring(1);
      }
      
      // Append "-qr-code" natively based on the language, or just use the tool slug
      // For maximum SEO, the translation file already says "Generador de codigo QR para WiFi",
      // so slugifying it gives "generador-de-codigo-qr-para-wifi".
      // Let's verify what `types` looks like. In some, `types.wifi` is just "WiFi".
      // Wait! `types.wifi` is the short name used in tabs. It doesn't contain "QR Code Generator".
      
      // Let's check what `types.wifi` actually is in English... it's "URL / Link" or "WiFi".
      // So the slug would just be "/wifi". Which is OK, but for SEO we want "wifi-qr-code-generator".
      // But we can append the translation of "QR Code Generator".
      
      const qrCodeGeneratorStr = data?.appTitle || "QR Code Generator";
      let finalSlug = robustSlugify(translatedTitle + ' ' + qrCodeGeneratorStr);
      
      const theSlug = '/' + finalSlug;
      
      // Ensure unique slugs
      let safeSlug = theSlug;
      let counter = 2;
      while (routeToToolMap[lang][safeSlug]) {
        safeSlug = theSlug + '-' + counter;
        counter++;
      }
      
      localizedRoutes[lang][type] = safeSlug;
      routeToToolMap[lang][safeSlug] = type;
    }
  }
  
  const fileContent = `// Auto-generated by generate-localized-routes-fast.cjs
export const localizedRoutes = ${JSON.stringify(localizedRoutes, null, 2)};
export const routeToToolMap = ${JSON.stringify(routeToToolMap, null, 2)};
`;
  
  fs.writeFileSync(path.join(__dirname, 'src', 'config', 'localizedRoutes.js'), fileContent, 'utf8');
  console.log("Successfully generated src/config/localizedRoutes.js!");
}

run();
